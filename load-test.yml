config:
  target: "http://localhost:3000"
  phases:
    - duration: 2
      arrivalCount: 20 # Simulate 20 users trying to reserve in 5 seconds
scenarios:
  - flow:
      - post:
          url: "/product/1/reserve" # Make sure '1' is the ID of your test product
